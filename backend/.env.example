# Optional: add a real Groq API key to use live AI responses.
# If this is left blank, the backend uses a local fallback summarizer/chat flow.
GROQ_API_KEY=

# Reserved for future database integration.
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=infera_ai
